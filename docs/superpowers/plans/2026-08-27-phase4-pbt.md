# 第 4 段階 — Property-Based Testing(hypothesis、0.6.0)実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** hypothesis でプロパティ 6 本(`tests/test_properties.py`)を書き、`default 25 / thorough 300 / mutation 5` のプロファイルで `quick` / `all` / mutation の予算に収め、各プロパティが mutation で最低 1 件の変異を殺すことを確認して 0.6.0 としてリリースする。

**Architecture:** プロパティは純関数・ファイル I/O 中心の `hooks/lib`(`config._validate` / `fingerprint.is_watched` / `fingerprint.compute` / `log` / `state`)に限定し、例ごとに一意な `root` を生成して autouse fixture の隔離ディレクトリ内で衝突を避ける。プロファイルは `tests/conftest.py` で登録し、`MUTANT_UNDER_TEST`(mutmut が設定)があれば自動で `mutation` を選ぶ。`scripts/verify.py` には `properties` ステージ(thorough)を足し、`all` = quick → properties → mutation。`quick` の `STAGES` と CI ミラーは不変。

**Tech Stack:** Python 3.10+、uv、pytest、hypothesis 6.165+、mutmut 3.7、ruff、pyright strict。

**Spec:** `docs/superpowers/specs/2026-08-27-phase4-pbt-design.md`(親: `2026-08-26-verification-roadmap-design.md` §5)

## Global Constraints

- `requires-python = ">=3.10"`。CI は 3.10 と 3.14。
- 入口ファイル(`hooks/gate.py`・`hooks/session_start.py`・`hooks/hooks.json`)と `hooks/lib` の**ソースは変えない**(プロパティが欠陥を見つけた場合だけ、失敗するプロパティ → 修正の順で最小限に直し、spec §3 に記録)。
- `quick` の `STAGES` は変えない。`properties` / `mutation` / `all` は Stop ゲート・CI に載せない。
- 予算: `quick` の増分 ≤ 2 秒、`properties`(thorough)≤ 60 秒、`mutation` の増分 ≤ 60 秒。超えたら例数を下げる(spec §5)。
- 生成する入力に実ホームパスを含めない(`root` は `/home/USER/pbt-<hex>` 形式)。ソース・コミットメッセージにも書かない。
- `@settings(deadline=None)` を必ず付ける(プロファイル既定でも明示)。hypothesis の `@given` は関数スコープ fixture と相性が悪いので、`tmp_path` を `@given` と併用しない(autouse の `CLAUDE_PLUGIN_DATA` 隔離だけに頼る)。
- 日本語の `test_…` 名、日本語 Conventional Commits、`uv run …` はリポジトリルートから。ゲートが掛かっている(`.py` / `.toml` / `.json` / `.github/**/*.yml` を変えたターンの終了時に `quick`)。

---

## ファイル構成

| ファイル | 責務 | 変更 |
|---|---|---|
| `tests/conftest.py` | hypothesis プロファイル登録・選択 | 変更 |
| `tests/test_properties.py` | プロパティ 6 本(P1〜P6) | 新規 |
| `pyproject.toml` | dev 依存 `hypothesis`、`.hypothesis` の除外 | 変更 |
| `.gitignore` / `.loop-hooks.json` | `.hypothesis/` の除外 | 変更 |
| `tests/test_packaging.py` | 除外設定の固定 | 変更 |
| `scripts/verify.py` / `tests/test_verify.py` | `properties` ステージ、`all` の順序 | 変更 |
| `CLAUDE.md` / README 英日 / CHANGELOG / バージョン 3 箇所 / spec | 0.6.0 | 変更 |

---

### Task 1: hypothesis の導入とプロファイル、`.hypothesis/` の除外

**Files:**
- Modify: `pyproject.toml`, `tests/conftest.py`, `.gitignore`, `.loop-hooks.json`, `tests/test_packaging.py`

**Interfaces:**
- Produces: プロファイル `default`(25)/ `thorough`(300)/ `mutation`(5)、`deadline=None`。環境変数 `HYPOTHESIS_PROFILE` で選択、`MUTANT_UNDER_TEST` があれば `mutation`。Task 2〜5 が依存。

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_packaging.py` の末尾:

```python
def test_hypothesisの除外設定():
    """spec §2.4: .hypothesis/(例のデータベース)は git・ruff・pyright・ゲートの対象外。"""
    cfg = tomllib.loads((ROOT / "pyproject.toml").read_text(encoding="utf-8"))
    assert ".hypothesis" in cfg["tool"]["ruff"]["extend-exclude"]
    assert ".hypothesis" in cfg["tool"]["pyright"]["exclude"]
    assert ".hypothesis/" in (ROOT / ".gitignore").read_text(encoding="utf-8").splitlines()
    gate = json.loads((ROOT / ".loop-hooks.json").read_text(encoding="utf-8"))["gate"]
    assert ".hypothesis/*" in gate["ignore"]
    assert any(d.startswith("hypothesis") for d in cfg["dependency-groups"]["dev"])


def test_hypothesisのプロファイルが登録されている():
    from hypothesis import settings

    for name, n in (("default", 25), ("thorough", 300), ("mutation", 5)):
        prof = settings.get_profile(name)
        assert prof.max_examples == n and prof.deadline is None, name
```

Run: `uv run pytest tests/test_packaging.py -q -k hypothesis` → FAIL(`KeyError` / `ModuleNotFoundError`)。

- [ ] **Step 2: 依存と除外**

`uv add --dev hypothesis`(6.165 以上)。`pyproject.toml`: `[tool.ruff] extend-exclude = ["docs", "mutants", ".hypothesis"]`、`[tool.pyright] exclude = ["mutants", ".hypothesis"]`。`.gitignore` に `.hypothesis/`(コメント `# hypothesis の例データベース`)。`.loop-hooks.json` の `ignore` を `[".superpowers/*", "docs/*", "mutants/*", ".hypothesis/*"]` に。

- [ ] **Step 3: conftest にプロファイル**

`tests/conftest.py` を次に置き換える:

```python
"""テストが実利用者の状態ディレクトリを汚さないようにする。hypothesis のプロファイルもここで選ぶ。"""

import os

import pytest
from hypothesis import settings

# spec §2.2: quick / CI は default(25 例)、`verify.py all` の properties ステージは thorough(300 例)、
# mutmut が各変異でテストを回すときは MUTANT_UNDER_TEST が付くので mutation(5 例)に自動で絞る。
settings.register_profile("default", max_examples=25, deadline=None)
settings.register_profile("thorough", max_examples=300, deadline=None)
settings.register_profile("mutation", max_examples=5, deadline=None)
settings.load_profile(
    "mutation" if os.environ.get("MUTANT_UNDER_TEST") else os.environ.get("HYPOTHESIS_PROFILE", "default")
)


@pytest.fixture(autouse=True)
def 状態ディレクトリを隔離する(tmp_path_factory, monkeypatch):
    monkeypatch.setenv("CLAUDE_PLUGIN_DATA", str(tmp_path_factory.mktemp("plugin-data")))
```

- [ ] **Step 4: 通す・コミット**

Run: `uv run pytest tests/test_packaging.py -q && uv run python scripts/verify.py quick`

```bash
git add pyproject.toml uv.lock tests/conftest.py .gitignore .loop-hooks.json tests/test_packaging.py
git commit -m "chore: hypothesis を導入し、プロファイル(default/thorough/mutation)と .hypothesis の除外を設定"
```

---

### Task 2: P1(`config._validate`)と P2(`fingerprint.is_watched`)

**Files:**
- Create: `tests/test_properties.py`

**Interfaces:**
- Consumes: `config._validate(raw: Any) -> dict[str, Any]`(`_error` か `gate`)、`config.TIMEOUT_MAX_SEC`、`config.EVENTS`、`fingerprint.is_watched(rel: str, gate_cfg: dict) -> bool`(`ignore` 優先、`fnmatch`)。
- Produces: `tests/test_properties.py` の骨格(import、`_root()` ヘルパ、戦略)。Task 3・4 が同ファイルに追記する。

- [ ] **Step 1: プロパティを書く(RED は「性質が成り立たない場合に落ちる」ことで示す)**

`tests/test_properties.py`:

```python
"""Property-Based Testing(hypothesis)。任意の入力に対して成り立つべき性質を固定する(第 4 段階 spec §2.1)。

例示テストは「思いついた入力」しか守れない。ここでは生成した入力で lib の不変条件を検査する。
例ごとに一意な root を使い、autouse の CLAUDE_PLUGIN_DATA 隔離の中で状態が衝突しないようにする。
"""

import fnmatch
import sys
import uuid
from pathlib import Path
from typing import Any

from hypothesis import assume, given, settings
from hypothesis import strategies as st

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from hooks.lib import config, fingerprint  # noqa: E402


def _root() -> str:
    """例ごとに一意なリポジトリパス(実在しなくてよい。state/log はキー化して外に書く)。"""
    return f"/home/USER/pbt-{uuid.uuid4().hex}"


# ---- P1: config._validate は任意の JSON 値で例外を出さず、_error か gate のどちらかを返す ----

_json_scalars = (
    st.none()
    | st.booleans()
    | st.integers()
    | st.floats(allow_nan=False, allow_infinity=False)
    | st.text(max_size=16)
)
_json_values = st.recursive(
    _json_scalars,
    lambda c: st.lists(c, max_size=4) | st.dictionaries(st.text(max_size=8), c, max_size=4),
    max_leaves=12,
)
_gate_like = st.fixed_dictionaries(
    {},
    optional={
        "command": _json_values,
        "timeout_sec": _json_values,
        "watch": _json_values,
        "ignore": _json_values,
        "on": _json_values,
    },
)
_raw_configs = _json_values | st.fixed_dictionaries({"gate": _gate_like | _json_values})


@settings(deadline=None)
@given(raw=_raw_configs)
def test_P1_validateは任意のJSONで例外を出さずerrorかgateを返す(raw: Any):
    result = config._validate(raw)
    assert isinstance(result, dict)
    if "_error" in result:
        assert isinstance(result["_error"], str) and result["_error"]
        assert "gate" not in result
        return
    gate = result["gate"]
    assert isinstance(gate["command"], str) and gate["command"].strip()
    assert isinstance(gate["timeout_sec"], int) and not isinstance(gate["timeout_sec"], bool)
    assert 1 <= gate["timeout_sec"] <= config.TIMEOUT_MAX_SEC
    for key in ("watch", "ignore"):
        assert isinstance(gate[key], list) and all(isinstance(v, str) for v in gate[key])
    assert isinstance(gate["on"], list) and gate["on"]
    assert all(v in config.EVENTS for v in gate["on"])


# ---- P2: is_watched — ignore が watch より優先し、watch に無いものは False ----

_segment = st.from_regex(r"[A-Za-z0-9_.-]{1,8}", fullmatch=True)
_rel_paths = st.lists(_segment, min_size=1, max_size=3).map("/".join)
_patterns = st.lists(
    st.one_of(_segment, _segment.map(lambda s: f"*.{s}"), _segment.map(lambda s: f"{s}/*"), st.just("*")),
    max_size=4,
)


@settings(deadline=None)
@given(rel=_rel_paths, watch=_patterns)
def test_P2a_ignoreに一致すればwatchに関係なくFalse(rel: str, watch: list[str]):
    cfg = {"watch": watch + [rel], "ignore": [rel]}
    assert fingerprint.is_watched(rel, cfg) is False


@settings(deadline=None)
@given(rel=_rel_paths, watch=_patterns)
def test_P2b_watchのどれにも一致しなければFalse(rel: str, watch: list[str]):
    assume(not any(fnmatch.fnmatch(rel, p) for p in watch))
    assert fingerprint.is_watched(rel, {"watch": watch, "ignore": []}) is False


@settings(deadline=None)
@given(rel=_rel_paths, watch=_patterns, ignore=_patterns)
def test_P2c_watchにrel自身がありignoreに一致しなければTrue(rel: str, watch: list[str], ignore: list[str]):
    assume(not any(fnmatch.fnmatch(rel, p) for p in ignore))
    assert fingerprint.is_watched(rel, {"watch": watch + [rel], "ignore": ignore}) is True
```

- [ ] **Step 2: 走らせる(性質が成り立つことを確認)**

Run: `uv run pytest tests/test_properties.py -q -p no:cacheprovider`
Expected: 4 passed。落ちたら hypothesis が最小反例を出す — **本物の欠陥か、性質の書き方の誤りかを切り分ける**(前者なら Global Constraints の手順で `hooks/lib` を直し、記録する)。

RED の代わりに「性質が効いていること」を 1 回確認する: `hooks/lib/fingerprint.py::is_watched` の `ignore` 判定を一時的に `return True` にして P2a が落ちることを見て、戻す(`git checkout hooks/lib/fingerprint.py`)。

- [ ] **Step 3: lint と時間**

Run: `uv run ruff check tests && uv run ruff format --check tests && time uv run pytest tests/test_properties.py -q`
Expected: clean、1 秒台。

- [ ] **Step 4: コミット**

```bash
git add tests/test_properties.py
git commit -m "test(pbt): config._validate と fingerprint.is_watched のプロパティ(P1, P2)"
```

---

### Task 3: P4・P5(`log`)と P6(`state`)

**Files:**
- Modify: `tests/test_properties.py`

**Interfaces:**
- Consumes: `log.append(root, record)`, `log.tail(root, n)`, `log._path(root)`, `log.MAX_LINES`, `log.KEEP_LINES`; `state.write_verified/read_verified`, `write_blocked/read_blocked`, `write_noticed/read_noticed`, `state._path(root)`。

- [ ] **Step 1: 追記する**

`tests/test_properties.py` の import に `from hooks.lib import log, state` を足し(1 行に統合: `from hooks.lib import config, fingerprint, log, state`)、末尾に:

```python
# ---- P4: log.tail は任意のバイト列で例外を出さず、list[dict] を n 件以下で返す ----


@settings(deadline=None)
@given(data=st.binary(max_size=2000), n=st.integers(min_value=1, max_value=20))
def test_P4_tailは任意のバイト列で落ちずn件以下のdictを返す(data: bytes, n: int):
    root = _root()
    p = log._path(root)
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_bytes(data)
    out = log.tail(root, n)
    assert isinstance(out, list) and len(out) <= n
    assert all(isinstance(r, dict) for r in out)


# ---- P5: append を k 回しても行数は上限内。超えた直後は KEEP_LINES 以上 ----


@settings(deadline=None, max_examples=15)
@given(k=st.integers(min_value=0, max_value=log.MAX_LINES + 300))
def test_P5_何回appendしても行数は上限内(k: int):
    root = _root()
    for i in range(k):
        log.append(root, {"event": "Stop", "decision": "skipped", "i": i})
    p = log._path(root)
    lines = len(p.read_text(encoding="utf-8").splitlines()) if p.exists() else 0
    assert lines <= log.MAX_LINES
    if k > log.MAX_LINES:
        assert lines >= log.KEEP_LINES
    else:
        assert lines == k


# ---- P6: state の round trip と、壊れたファイルは None ----

_fp_text = st.text(alphabet=st.characters(blacklist_characters="\x00"), max_size=64)


@settings(deadline=None)
@given(verified=_fp_text, blocked=_fp_text, noticed=_fp_text)
def test_P6a_書いた値がそのまま読め互いに干渉しない(verified: str, blocked: str, noticed: str):
    root = _root()
    state.write_verified(root, verified)
    state.write_blocked(root, blocked)
    state.write_noticed(root, noticed)
    assert state.read_verified(root) == verified
    assert state.read_blocked(root) == blocked
    assert state.read_noticed(root) == noticed


@settings(deadline=None)
@given(data=st.binary(max_size=500))
def test_P6b_壊れた状態ファイルは例外を出さずNone(data: bytes):
    root = _root()
    p = state._path(root)
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_bytes(data)
    assert state.read_verified(root) is None or isinstance(state.read_verified(root), str)
    assert state.read_blocked(root) is None or isinstance(state.read_blocked(root), str)
```

P6b は「JSON として偶然 `{"verified": "..."}` を含むバイト列」も生成しうるので、None **または** str を許す(例外を出さないことが性質)。

- [ ] **Step 2: 走らせて時間を見る**

Run: `time uv run pytest tests/test_properties.py -q`
Expected: 8 passed、3 秒以内(P5 が支配的。超えるなら `max_examples=10`)。

- [ ] **Step 3: コミット**

```bash
git add tests/test_properties.py
git commit -m "test(pbt): log の tail・切詰めと state の round trip のプロパティ(P4, P5, P6)"
```

---

### Task 4: P3(`fingerprint.compute` の決定性と感度)

**Files:**
- Modify: `tests/test_properties.py`

**Interfaces:**
- Consumes: `fingerprint.compute(root: str, gate_cfg: dict) -> str | None`(git リポジトリが必要)。

- [ ] **Step 1: モジュール fixture とプロパティを追記する**

`tests/test_properties.py` の import に `import subprocess` と `import pytest` を足し、末尾に:

```python
# ---- P3: compute は決定的で、watch 外の変更に不変、watch 内の変更に敏感 ----

_P3_CFG = {"watch": ["*.py"], "ignore": ["*.md"]}


def _git(cwd: Path, *args: str) -> None:
    subprocess.run(("git", *args), cwd=cwd, capture_output=True, check=True)


@pytest.fixture(scope="module")
def p3_repo(tmp_path_factory) -> Path:
    """P3 用の git リポジトリ(モジュールで 1 回だけ作る。例ごとに 2 ファイルの内容だけ書き換える)。"""
    repo = tmp_path_factory.mktemp("p3-repo")
    _git(repo, "init", "-q")
    _git(repo, "config", "user.email", "t@example.com")
    _git(repo, "config", "user.name", "t")
    _git(repo, "config", "commit.gpgsign", "false")
    (repo / "watched.py").write_bytes(b"initial\n")
    (repo / "unwatched.md").write_bytes(b"initial\n")
    _git(repo, "add", "-A")
    _git(repo, "commit", "-qm", "init")
    return repo


@settings(deadline=None, max_examples=10)
@given(a=st.binary(max_size=64), b=st.binary(max_size=64))
def test_P3_computeは決定的でwatch外に不変watch内に敏感(p3_repo: Path, a: bytes, b: bytes):
    root = str(p3_repo)
    (p3_repo / "unwatched.md").write_bytes(a)
    (p3_repo / "watched.py").write_bytes(b)
    fp1 = fingerprint.compute(root, _P3_CFG)
    fp2 = fingerprint.compute(root, _P3_CFG)
    assert fp1 is not None and fp1 == fp2  # 決定性
    (p3_repo / "unwatched.md").write_bytes(a + b"x")
    assert fingerprint.compute(root, _P3_CFG) == fp1  # watch 外は不変
    (p3_repo / "watched.py").write_bytes(b + b"x")
    assert fingerprint.compute(root, _P3_CFG) != fp1  # watch 内は変わる
```

`p3_repo` はモジュールスコープなので `@given` と併用できる(関数スコープだと hypothesis が警告する)。

- [ ] **Step 2: 走らせて時間を見る**

Run: `time uv run pytest tests/test_properties.py -q`
Expected: 9 passed。P3 は 10 例 × git 呼出 4 回 × 4 計算 ≈ 1〜2 秒。全体で `quick` 増分 2 秒以内に収まるか `time uv run python scripts/verify.py quick` で確認(Task 1 前の実測 12.4 秒と比較)。超えるなら P3 を 5 例、P5 を 10 例に。

- [ ] **Step 3: コミット**

```bash
git add tests/test_properties.py
git commit -m "test(pbt): fingerprint.compute の決定性と感度のプロパティ(P3)"
```

---

### Task 5: `scripts/verify.py` の `properties` ステージと `all` の順序

**Files:**
- Modify: `scripts/verify.py`, `tests/test_verify.py`

**Interfaces:**
- Produces: `verify.PROPERTIES_CHECK: Check`、`verify.run_properties(repo_root: Path = REPO_ROOT) -> bool`、`main(["properties"])`、`all` = quick → properties → mutation。

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_verify.py` の `test_mainはmutationとallを受け付ける` を次に置き換え、テストを 2 本足す:

```python
def test_mainはproperties_mutation_allを受け付ける(monkeypatch):
    calls: list[str] = []
    monkeypatch.setattr(
        verify, "run_stage", lambda stage, checks=None, repo_root=None: calls.append(stage) or True
    )
    monkeypatch.setattr(verify, "run_mutation", lambda: calls.append("mutation") or True)
    assert verify.main(["mutation"]) == 0 and calls == ["mutation"]
    calls.clear()
    assert verify.main(["properties"]) == 0 and calls == ["properties"]
    calls.clear()
    assert verify.main(["all"]) == 0 and calls == ["quick", "properties", "mutation"]


def test_mainのallはpropertiesが落ちればmutationを回さない(monkeypatch):
    calls: list[str] = []
    monkeypatch.setattr(
        verify,
        "run_stage",
        lambda stage, checks=None, repo_root=None: calls.append(stage) or stage != "properties",
    )
    monkeypatch.setattr(verify, "run_mutation", lambda: calls.append("mutation") or True)
    assert verify.main(["all"]) == 1 and calls == ["quick", "properties"]


def test_propertiesチェックはthoroughプロファイルでtest_propertiesだけを回す():
    c = verify.PROPERTIES_CHECK
    assert c.cmd == ["uv", "run", "pytest", "-q", "tests/test_properties.py"]
    assert dict(c.env) == {"HYPOTHESIS_PROFILE": "thorough"}
    assert "properties" not in [x.name for x in verify.STAGES["quick"]]
```

Run: `uv run pytest tests/test_verify.py -q -k "properties or allは"` → FAIL(`AttributeError` / usage 2)。

- [ ] **Step 2: 実装**

`scripts/verify.py`: `STAGES` の下に

```python
# spec 第 4 段階 §2.3: hypothesis を 300 例で回す。quick(25 例)と CI には載せない
PROPERTIES_CHECK = Check(
    "properties",
    ["uv", "run", "pytest", "-q", "tests/test_properties.py"],
    env=(("HYPOTHESIS_PROFILE", "thorough"),),
)


def run_properties(repo_root: Path = REPO_ROOT) -> bool:
    return run_stage("properties", [PROPERTIES_CHECK], repo_root)
```

`main`:

```python
def main(argv: Sequence[str]) -> int:
    stages = [*STAGES, "properties", "mutation", "all"]
    if len(argv) != 1 or argv[0] not in stages:
        print(f"usage: verify.py {{{'|'.join(stages)}}}", file=sys.stderr)
        return 2
    if argv[0] == "properties":
        return 0 if run_properties() else 1
    if argv[0] == "mutation":
        return 0 if run_mutation() else 1
    if argv[0] == "all":
        return 0 if run_stage("quick") and run_properties() and run_mutation() else 1
    return 0 if run_stage(argv[0]) else 1
```

`run_stage` はステージ名を `STAGES` から引くので、`checks` を渡す呼び方はそのまま使える(`stage` 引数は表示用にだけ使われる — 実装を読んで確認し、`STAGES[stage]` を無条件で引いていたら `checks is None` のときだけ引くように直す)。docstring の `all` の説明を「quick → properties → mutation」に。

- [ ] **Step 3: 通す・実測・コミット**

Run: `uv run pytest tests/test_verify.py -q && time uv run python scripts/verify.py properties`
Expected: pass、`[verify] properties: ok`、**60 秒以内**(thorough 300 例。超えるなら P3 / P5 の個別 `max_examples` はそのまま、`thorough` を 200 に下げて spec に記録)。

```bash
git add scripts/verify.py tests/test_verify.py
git commit -m "feat(verify): properties ステージ(hypothesis thorough)と all = quick → properties → mutation"
```

---

### Task 6: 証明力の確認(mutation)、文書、0.6.0

**Files:**
- Modify: `tests/mutation-baseline.json`(ランナー)、`CLAUDE.md`, `README.md`, `README.ja.md`, `CHANGELOG.md`, `pyproject.toml`, `.claude-plugin/plugin.json`, `uv.lock`, spec §3

- [ ] **Step 1: mutation の所要時間と killed の変化**

Run: `time uv run python scripts/verify.py mutation; echo exit=$?`
Expected: exit 0、所要時間を記録(第 3 段階の 64 秒 + 増分 ≤ 60 秒)。killed が増えたファイルは baseline がランナーで上がる。`git diff tests/mutation-baseline.json` を記録。超過なら `mutation` プロファイルを 3 例に。

- [ ] **Step 2: 各プロパティが最低 1 件殺すことの確認**

6 本の集合ごとに、`tests/test_properties.py` の他のプロパティを `@pytest.mark.skip` で一時的に外し(P2a/b/c は 1 組、P6a/b は 1 組として 6 組)、`uv run python scripts/verify.py mutation` を回して該当ファイルの killed を「プロパティ全部無し」の場合と比べる。手順:
  1. `git stash` は使わず、ファイル先頭に `pytestmark = pytest.mark.skip` を置いた状態で 1 回(全プロパティ無し)→ killed を記録(= 第 3 段階の値と一致するはず)。
  2. 6 組それぞれについて、その組だけ skip を外して 1 回ずつ(各 ≈ 1〜2 分)。
  3. 組ごとの killed 増分を表にして spec §3 に記録。**増分 0 の組は性質を書き直す**(例: P1 で `_validate` の分岐を殺せないなら、`_error` 文言の固定や既定値の補完をより強く検査する)。
  最後に skip を全部外し、`uv run python scripts/verify.py mutation` を回して baseline を確定(この run の baseline をコミット)。

- [ ] **Step 3: 文書とバージョン**

- `CLAUDE.md` 開発節: `all` の説明を「quick + properties(hypothesis 300 例)+ mutation(約 3〜5 分)」に、「mutation 中は hypothesis を 5 例に自動で絞る(`MUTANT_UNDER_TEST`)」を追加。
- `README.md` Tests 節に一文: `tests/test_properties.py holds hypothesis property tests (25 examples in the gate and CI, 300 in \`verify.py all\`).` `README.ja.md` に対応文。
- `CHANGELOG.md` 先頭:

```markdown
## [0.6.0] - 2026-08-27

### Added
- **Property-based tests** (`tests/test_properties.py`, hypothesis): configuration validation
  never raises on arbitrary JSON; `is_watched` honours `ignore` over `watch`; the fingerprint is
  deterministic, blind to unwatched files and sensitive to watched ones; the decision log tolerates
  arbitrary bytes and keeps its size bounded; state round-trips and degrades to `None` on corruption.
  25 examples in the gate and CI, 300 in `verify.py all` (`properties` stage), 5 while mutmut runs.
- `scripts/verify.py properties`; `all` is now quick → properties → mutation.

### Upgrading
- Nothing to do. No entry-point files or hook definitions changed; no restart needed.
```

- バージョン `0.6.0`(`pyproject.toml` / `plugin.json`、`uv lock`)。
- spec §3 の末尾に「確認済み(日付): quick 増分 N 秒 / properties M 秒 / mutation 増分 K 秒 / 組ごとの killed 増分表 / 発見した欠陥(あれば)」。

- [ ] **Step 4: 全体検証・コミット**

Run: `uv run python scripts/verify.py all; echo exit=$?` → exit 0(所要時間も記録)。

```bash
git add tests/mutation-baseline.json CLAUDE.md README.md README.ja.md CHANGELOG.md pyproject.toml .claude-plugin/plugin.json uv.lock docs
git commit -m "chore: 0.6.0 のリリース準備(PBT の運用を文書化、mutation baseline 更新)"
```

---

### Task 7: 受け入れ(main マージ後)

- [ ] push 後、CI の `test`(3.10 / 3.14)と `security` が green(default プロファイル 25 例で CI の時間が伸びていないことを確認)。
- [ ] spec と親 spec のレビュー状況表を「第 4 段階 完了(0.6.0)」に、計画を `docs/superpowers/archive/plans/` へ。

```bash
git mv docs/superpowers/plans/2026-08-27-phase4-pbt.md docs/superpowers/archive/plans/
git add docs && git commit -m "docs: 第 4 段階の受け入れ結果を記録し、計画をアーカイブ"
```

---

## 自己レビュー

**Spec coverage:** §2.1 P1〜P6 → Task 2/3/4 / §2.2 → Task 1 / §2.3 → Task 5 / §2.4 → Task 1 / §2.5 → Task 6 Step 2 / §2.6 → Task 6 Step 3 / §3 → Task 4 Step 2(quick 増分)、Task 5 Step 3(properties)、Task 6 Step 1(mutation 増分)、Task 7。

**判断(spec に無いもの):** (1) P6b は None **または** str を許す(生成バイト列が偶然 JSON になりうる)。(2) 証明力の確認は「組ごとに skip を外して 6 回」方式(mutmut は殺したテスト名を出さないため)。(3) `run_stage` の `stage` 引数が `STAGES` を無条件に引く実装なら Task 5 で直す。

**型整合:** `verify.PROPERTIES_CHECK: Check`、`verify.run_properties(repo_root=REPO_ROOT) -> bool`、`_root() -> str`、`p3_repo: Path`(module fixture)、`_P3_CFG` は各 Task で同名。
